<h1>Listing hyperlinks</h1>

<table>
  <tr>
    <th>[submitted hyperlink</th>
  </tr>

<% @hyperlinks.each do |hyperlink| %>
  <tr>
    <td><%=h hyperlink.[submitted_hyperlink %></td>
    <td><%= link_to 'Show', hyperlink %></td>
    <td><%= link_to 'Edit', edit_hyperlink_path(hyperlink) %></td>
    <td><%= link_to 'Destroy', hyperlink, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New hyperlink', new_hyperlink_path %>